{
  "id": "xml_convert",
  "version": "1.1.0",
  "title": "XML import",
  "description": "Imports XML exports from the xml/ directory into the DuckDB database.",
  "author": "fm-lab core",
  "icon": "database",
  "tags": ["system"],
  "entry": "layout.json",
  "datasets": [
    { "id": "directory_status",     "source": "builtin:xml_directory_status" },
    { "id": "last_run",             "source": "builtin:xml_last_run" },
    { "id": "xml_import_integrity",         "source": "builtin:xml_import_integrity" },
    { "id": "xml_semantic_names",   "source": "builtin:xml_semantic_names" }
  ],
  "params": [
    { "name": "autostart", "type": "string", "required": false,
      "description": "Wenn '1', startet die Konvertierung automatisch beim Mount." },
    { "name": "solution_id", "type": "string", "required": false,
      "description": "Kontext-Lösung der Import-Seite; fehlt der Parameter, gilt die aktive Lösung." }
  ],
  "permissions": { "read_only": false, "allow_navigation": true }
}
