{
  "name": "fm-lab",
  "version": "0.9.10",
  "description": "FM-Lab - Open Source framework for agentic FileMaker development",
  "private": true,
  "workspaces": [
    "rest-api",
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=rest-api",
    "dev:shared": "npm run build:watch --workspace=packages/shared",
    "build:shared": "npm run build --workspace=packages/shared",
    "generate:types": "npm run generate --workspace=packages/shared",
    "install:all": "npm install && npm install --workspaces",
    "clean": "npm run clean --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "keywords": [
    "filemaker",
    "duckdb",
    "xml",
    "analysis",
    "monorepo"
  ],
  "author": "Marcel Moré",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "graphology": "^0.26.0",
    "graphology-communities-louvain": "^2.0.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
